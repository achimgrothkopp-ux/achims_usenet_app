# GUI
PySide6>=6.7
qasync>=0.27

# NNTP (nntplib ist in Python 3.13 entfernt)
pynntp>=2.0

# HTTP-Client für SABnzbd-API und spaeter Newznab-Indexer
httpx>=0.27

# NZB-XML
lxml>=5.2

# TOML schreiben (tomllib in stdlib ist nur read-only)
tomli-w>=1.0

# yEnc-Decoder (C-Extension, von SABnzbd-Entwicklern). Für Bild-Vorschau
# in der ArticleView – dekodiert Single- und Multi-Part-yEnc.
sabyenc3>=5.4
